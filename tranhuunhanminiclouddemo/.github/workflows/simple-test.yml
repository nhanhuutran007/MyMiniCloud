name: Simple Test

on:
  push:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Hello World
        run: echo "Hello from GitHub Actions!"
        
      - name: Show files
        run: ls -la
        
      - name: Test completed
        run: echo "✅ Simple test completed successfully!"